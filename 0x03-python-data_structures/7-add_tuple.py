#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    len_t1 = len(tuple_a)
    len_t2 = len(tuple_b)
    if (len_t1 < 2 or len_t2 < 2):
        if len_t1== 1:
            b1 = 0
        if len(tuple_a) == 0:
            a1 == 0
        if len(tuple_b) == 1:
            b2 = 0
        if len(tuple_b) == 0:
            a2 == 0
    else:
        a
    return (a1 + a2, b1 + b2)
